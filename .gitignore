.DS_Store
*.swp
*.swo
.claude/todo/

# ユーザーローカル設定（install.sh が config.default.json から初回生成する個人用ファイル）
config.json

# CONDUCTOR_HOME としてリポジトリ／worktree を使う場合（mdev-test 等）の実行時生成物
/bin/
/daily/
/news/
/tasks/
/upload-cache/
/VERSION

# Go のビルド成果物（リリース資産は CI が dist/ に吐く）
/dist/
